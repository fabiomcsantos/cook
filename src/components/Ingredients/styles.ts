import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        height: 58,
        maxHeight: 58,
    },
    ingredientsContent: {
        gap: 12,
        paddingHorizontal: 32,
    },
});

export { styles };